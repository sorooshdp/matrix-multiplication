[package]
name = "rust_faer"
version = "0.1.0"
edition = "2024"

[dependencies]
faer = "0.24"
winapi = { version = "0.3", features = ["psapi", "processthreadsapi"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
